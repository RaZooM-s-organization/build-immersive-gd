{
	"geode": "4.9.0",
	"gd": {
		"win": "2.2074"
	},
	"id": "razoom.s_pov",
	"name": "RaZooM's POV",
	"version": "v1.0.0-beta.2",
	"developer": "RaZooM",
	"description": "Get inside the GD and control it with your body!",

	"tags": ["gameplay", "content", "modtober25", "joke"],

	"dependencies": {
		"geode.node-ids": ">=v1.20.0"
	},

	"resources": {
		"files": [
			"resources/models/hrnet_coco_w32_256x192.onnx"
		],
		"sprites": [
			"resources/*.png"
		]
	},

	"links": {
		"source": "https://github.com/RazoomGD/geode-modtober-25-mod",
		"community": "https://discord.gg/wcWvtKHP8n"
	},

	"settings": {
		"video-capture-title": {
			"type": "title",
			"name": "Video Capture"
		},
		"device-name": {
			"type": "custom:razoom.s_pov/select-camera",
			"name": "Video Device",
			"description": "Camera that is used for capturing the video",
			"default": "(auto)"
		},
		"quality-multiplier":{
			"type": "float",
			"name": "Video Quality",
			"description": "If you have a high-resolution camera and video lags, try to decrease this value",
			"default": 1.0,
			"min": 0.1,
			"max": 1.0,
			"control": {
				"arrows": false,
				"big-arrows": false,
				"slider": true,
				"slider-step": 0.01
			}
		},


		"video-output-title": {
			"type": "title",
			"name": "Video Output"
		},
		"video-output-enable": {
			"type": "bool",
			"name": "Enable",
			"default": true,
			"description": "Translate the video to the background of the level"
		},
		"video-output-mirror": {
			"type": "bool",
			"name": "Mirror video",
			"default": true,
			"description": "Mirror the output video, just like your phone's camera does",
			"enable-if": "video-output-enable"
		},
		"video-output-fps-limit": {
			"type": "int",
			"name": "FPS Limit",
			"description": "Maximum refresh rate of the video",
			"default": 60,
			"min": 5,
			"max": 240,
			"control": {
				"arrows": true,
				"big-arrows": false,
				"slider": true
			},
			"enable-if": "video-output-enable"
		},
		"video-output-layer": {
			"type": "string",
			"name": "Layer",
			"description": "Layer above which the video is shown",
			"default": "BG",
			"one-of": ["BG", "B5", "B4", "B3", "B2", "B1", "B0", "T1", "T2", "T3", "T4"],
			"enable-if": "video-output-enable"
		},
		

		"pose-estimation-title":{
			"type": "title",
			"name": "Pose Estimation"
		},
		"pose-estimation-enable": {
			"type": "bool",
			"name": "Enable",
			"default": true,
			"description": "Enable pose estimation and game control with the pose"
		},
		"pose-estimation-num-of-threads": {
			"type": "int",
			"name": "Number Of Threads",
			"description": "Pose estimation is a complex task uses the neural network and requires many computational resources. That is why it is executed in parallel in several threads. <cg>The more threads it uses the faster it works</c>",
			"default": 5,
			"min": 1,
			"max": 10,
			"control": {
				"arrows": true,
				"big-arrows": false,
				"slider": true
			},
			"enable-if": "pose-estimation-enable"
		},
		"pose-estimation-detection-threshold": {
			"type": "float",
			"name": "Detection Threshold",
			"description": "Increase this value lower <cb>False Positives</c>. Decrease to lower <cb>False Negatives</c>.\nIn other words: '<co>How confident does a neural network have to be in order to accept a certain point of your body</c>'.\nI recommend keeping it between 0.3 and 0.6",
			"default": 0.5,
			"min": 0.1,
			"max": 0.9,
			"control": {
				"arrows": true,
				"big-arrows": false,
				"slider": true,
				"arrow-step": 0.1
			},
			"enable-if": "pose-estimation-enable"
		},
		"pose-estimation-video-output-fps-limit": {
			"type": "int",
			"name": "FPS Limit",
			"description": "Maximum refresh rate of the pose estimation",
			"default": 30,
			"min": 10,
			"max": 60,
			"control": {
				"arrows": true,
				"big-arrows": false,
				"slider": true
			},
			"enable-if": "pose-estimation-enable"
		},
		"pose-estimation-debug-draw": {
			"type": "bool",
			"name": "Show the pose",
			"description": "Show estimated pose when playing a level. \n <cb>I promise to make it less ugly in the future</c>",
			"default": true,
			"enable-if": "pose-estimation-enable"
		},
		"pose-estimation-disable-inputs": {
			"type": "bool",
			"name": "Disable inputs",
			"description": "Disable keyboard/mouse inputs while playing a level",
			"default": true,
			"enable-if": "pose-estimation-enable"
		},


		"gpu-title": {
			"type": "title",
			"name": "GPU Options"
		},
		"gpu-enable": {
			"type": "bool",
			"name": "GPU Acceleration",
			"description": "This increases the performance of pose estimation, but can make the overall game performance worse (since the game engine also needs a GPU)\n<cg>If option is disabled, it runs on CPU</c>",
			"default": true
		},
		"gpu-device-id": {
			"type": "int",
			"name": "GPU device id",
			"description": "If you have <co>more than 1 GPU</c>, you may want to change this number to change the GPU used by the mod.\n<cr>If you enter the number of GPU that doesn't exist, it will run on CPU</c>",
			"default": 0,
			"min": 0,
			"max": 10,
			"control": {
				"arrows": false,
				"big-arrows": false,
				"slider": false,
				"input": true
			},
			"enable-if": "gpu-enable"
		}


	}
}
